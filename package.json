{
  "dependencies": {
    "prando": "^6.0.1",
    "three": "^0.175.0"
  },
  "devDependencies": {
    "dotenv": "^16.5.0",
    "ftp-deploy": "^2.4.7",
    "vite": "^6.2.6"
  },
  "scripts": {
    "deploy": "npm run build && node scripts/deploy.js",
    "build": "vite build",
    "dev": "vite --port 4000"
  }
}